services:
  app:
    build: .
    container_name: selys-careers
    restart: always
    ports:
      - "6100:6100"
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads

volumes:
  uploads:
